{
  "name": "scalable-chess",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "clean": "npx rimraf node_modules/chess-core node_modules/chess-server && npm i",
    "build": "npm run clean && npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}